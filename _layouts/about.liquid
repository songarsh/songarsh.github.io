---
layout: default
---
<div class="post">
  {% comment %}
    Custom about header: photo on top, name + role + address + social icons
    centered below it. Bio and other sections follow as subsections.
  {% endcomment %}
  <header class="about-header about-header--centered">
    {% if page.profile and page.profile.image %}
      {% assign profile_image_path = page.profile.image | prepend: 'assets/img/' %}
      {% if page.profile.image_circular %}
        {% assign profile_image_class = 'img-fluid z-depth-1 rounded-circle about-header-img' %}
      {% else %}
        {% assign profile_image_class = 'img-fluid z-depth-1 rounded about-header-img' %}
      {% endif %}
      <div class="about-header-photo">
        {%
          include figure.liquid loading="eager" path=profile_image_path class=profile_image_class
          sizes="(min-width: 768px) 220px, 60vw" alt=page.profile.image cache_bust=true
        %}
      </div>
    {% endif %}

    <div class="about-header-text">
      <h1 class="post-title">
        {% if site.title == 'blank' %}
          <span class="font-weight-bold">{{ site.first_name }}</span> {{ site.middle_name }}
          {{ site.last_name }}
        {% else %}
          {{ site.title }}
        {% endif %}
      </h1>
      <p class="desc">{{ page.subtitle }}</p>
      {% if page.profile and page.profile.more_info %}
        <div class="more-info">{{ page.profile.more_info }}</div>
      {% endif %}
      {% if page.social %}
        <div class="contact-icons about-header-icons">{% social_links %}</div>
      {% endif %}
    </div>
  </header>

  <article>
    <!-- Bio -->
    <h2>Bio</h2>
    {{ content }}

    <!-- News -->
    {% if page.announcements and page.announcements.enabled %}
      <h2>
        <a href="{{ '/news/' | relative_url }}" style="color: inherit">News</a>
      </h2>
      {% include news.liquid limit=true %}
    {% endif %}

    <!-- Latest posts -->
    {% if page.latest_posts and page.latest_posts.enabled %}
      <h2>
        <a href="{{ '/blog/' | relative_url }}" style="color: inherit">Latest Posts</a>
      </h2>
      {% include latest_posts.liquid %}
    {% endif %}

    <!-- Selected papers -->
    {% if page.selected_papers %}
      <h2>
        <a href="{{ '/publications/' | relative_url }}" style="color: inherit">Selected Publications</a>
      </h2>
      {% include selected_papers.liquid %}
    {% endif %}

    {% if site.newsletter and site.newsletter.enabled and site.footer_fixed %}
      {% include newsletter.liquid center=true %}
    {% endif %}
  </article>
</div>
